# AXI4 Full 2M3S 验证项目

本项目针对一个 AXI4 Full 互连模块 进行验证，该模块包含：

- 2 个主设备（Masters）
- 3 个从设备（Slaves）

验证环境基于 UVM 架构开发。

本项目的主要验证目标包括：

- AXI protocol compliance ：
验证 DUT 是否符合 AXI4 Full 协议规范，包括握手流程、通道时序、响应机制等。
- Multi-master arbitration ：
验证多个 Master 同时访问共享资源时，互连模块是否能够正确进行仲裁，保证事务正确执行。
- Address decoding
验证不同地址空间是否能够正确映射到对应的 Slave，确保数据访问路径正确。
- Burst transaction handling ：
验证 AXI Burst 传输功能，包括不同 Burst 长度、类型以及连续数据传输的正确性。
- Outstanding transaction support ：
验证 DUT 是否支持多个未完成事务同时存在，以及事务管理和返回处理是否正确。
- Out-of-order response handling ：
验证在多个事务并发情况下，返回响应顺序与 AXI ID 匹配机制是否正确。
- Backpressure robustness ：
验证 Master 或 Slave 在无法立即接收数据时，DUT 是否能够正确处理 READY/VALID 握手暂停情况。
- Error response handling ：
验证当发生非法地址访问或异常情况时，DUT 是否能够产生正确的错误响应。

# DUT介绍

```
             +------------------+
M0 AXI ----->|                  |
             |                  |----> S0
M1 AXI ----->| AXI Interconnect |
             |                  |----> S1
             |                  |----> S2
             +------------------+
```